// 추천 프롬프트·후속 요청 자유 문구의 글자 상한. AI 매칭/생성 프롬프트에 그대로 실리므로
// 토큰·악용을 막으려 자른다. 서버(actions.ts)가 이 값으로 slice 하고, 클라이언트 입력창은
// 같은 값으로 maxLength·카운터를 걸어 말없이 잘리지 않게 한다 — 두 곳이 어긋나지 않도록 한 군데.
export const MAX_USER_PROMPT = 200;
