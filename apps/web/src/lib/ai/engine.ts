// 테스트를 써주는 엔진.
//
// 예전에는 사용자가 프로바이더를 고르고 자기 키를 넣었다(BYOK). 지금은 Dante 가
// 프로바이더와 직접 계약하고 엔진은 하나로 고정돼 있다. 고를 것이 없어졌으니
// 이 파일에 남는 건 "무엇이 붙어 있는지"를 화면에 적기 위한 상수 하나뿐이다.
// 그래서 자리도 lib/projects/ 가 아니라 lib/ai/ 다 — 프로젝트마다 고르는 값이
// 아니라 제품 전체의 사실이다.
//
// 모델 id 는 여기 적지 않는다. 화면에 나가는 이름과 실제로 부르는 모델은 수명이
// 다르다 — 모델은 몇 달마다 바뀌지만 그때마다 이 이름을 고칠 이유는 없다.
// 어떤 모델을 부를지는 chat-model.ts 한 곳에서만 정한다.

export const ENGINE = {
  /** 화면에 그대로 나가는 이름. 사용자는 회사가 아니라 이 이름으로 기억한다. */
  name: "Codex",
  /** 이름만으로 누구 모델인지 모르는 사용자를 위한 보조 표기. */
  vendor: "OpenAI",
} as const;
