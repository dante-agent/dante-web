# DESIGN.md

Dante 디자인 시스템 초안. **Brand colors / Supporting palette / Typography** 세 영역만 정리한다.
값은 [CodeRabbit brand guidelines](https://www.coderabbit.ai/brand) (2026-09-08 기준)를 참조해 가져온 것이고,
로고·마크·브랜드 락업은 가져오지 않는다 (상표).

스페이싱·radius·shadow·컴포넌트 스펙은 이 문서 범위 밖. shadcn 기본값(`apps/web/src/app/globals.css`)을 그대로 쓴다.

---

## 1. Brand colors

핵심 3색. 강조·상태·포인트에만 쓰고 넓은 면적은 뉴트럴(§2 Mauve)로 채운다.

| 이름      | Hex       | RGB             | 역할                                 |
| --------- | --------- | --------------- | ------------------------------------ |
| CR Orange | `#FF570A` | `255, 87, 10`   | Primary. CTA, 활성 상태, 브랜드 강조 |
| CR Mint   | `#25E2A8` | `37, 226, 168`  | Secondary. 성공, 통과, 긍정 피드백   |
| CR Cobalt | `#687FF5` | `104, 127, 245` | Tertiary. 링크, 정보, 보조 강조      |

각 색은 §2의 6단계 램프에서 **9번**이 기준값이다 (Mint만 램프 9번 `#46E1A5`이 브랜드값과 미세하게 다름 → 브랜드값 우선).

---

## 2. Supporting palette

### Mauve — 뉴트럴 (다크 전용, 1 → 12)

배경·표면·보더·텍스트. 어두운 쪽이 낮은 번호.

| #   | Hex       | 용도 예시             |
| --- | --------- | --------------------- |
| 1   | `#121014` | 페이지 배경           |
| 2   | `#1A181D` | 카드·표면             |
| 3   | `#232127` | 표면 hover            |
| 4   | `#2B282F` | 보더 (낮은 대비)      |
| 5   | `#322F37` | 보더                  |
| 6   | `#3C3841` | 보더 (강조)           |
| 7   | `#4A464F` | disabled 텍스트       |
| 8   | `#625E68` | placeholder           |
| 9   | `#6F6B75` | muted 텍스트          |
| 10  | `#7D7982` | secondary 텍스트      |
| 11  | `#B5B2B9` | body 텍스트           |
| 12  | `#EEEDF0` | heading·고대비 텍스트 |

> 라이트 모드 없음. 앱은 다크 단일 — 토글/`prefers-color-scheme` 대응 안 한다.

### 강조 램프 (각 6단계)

CodeRabbit이 공개한 스텝 번호만 존재. 표면 틴트 → 진한 배경 → 기준색 → 밝은 hover 순.

**Primary (Orange)**

| step | 2         | 4         | 6         | 8         | 9 (기준)  | 10        |
| ---- | --------- | --------- | --------- | --------- | --------- | --------- |
| hex  | `#1E150F` | `#462100` | `#66350C` | `#A35829` | `#FF570A` | `#FF801F` |

**Secondary (Mint)**

| step | 1         | 3         | 5         | 7         | 9 (기준)  | 11        |
| ---- | --------- | --------- | --------- | --------- | --------- | --------- |
| hex  | `#09120E` | `#062619` | `#003E27` | `#006A47` | `#46E1A5` | `#85F9C5` |

**Tertiary (Cobalt)**

| step | 1         | 3         | 5         | 7         | 9 (기준)  | 11        |
| ---- | --------- | --------- | --------- | --------- | --------- | --------- |
| hex  | `#0D0F16` | `#171D37` | `#242E60` | `#3A499C` | `#687FF5` | `#95AFFF` |

### Cream — 콘텐츠/마케팅용 (1 → 6)

제품 UI가 아니라 문서·랜딩·메일용 따뜻한 뉴트럴.

| #   | 1         | 2         | 3         | 4         | 5         | 6         |
| --- | --------- | --------- | --------- | --------- | --------- | --------- |
| hex | `#311C15` | `#6E564D` | `#A1887E` | `#DACDC6` | `#EBE7E6` | `#F7F2EF` |

---

## 3. Typography

| 역할    | 서체  | 굵기                           | 쓰는 곳                                    |
| ------- | ----- | ------------------------------ | ------------------------------------------ |
| Primary | Geist | 600 (heading) / 400 (본문)     | heading, 본문, 네비, CTA, 모든 읽는 텍스트 |
| Utility | Hack  | 700 (라벨·메트릭) / 400 (코드) | 코드, 데이터, 메타데이터, 짧은 기술 라벨   |

- **Geist**: SIL OFL 1.1, 무료. `next/font/google`의 `Geist` (별도 패키지 불필요).
- **Hack**: MIT + Bitstream Vera, 무료. npm `hack-font` (`@fontsource/hack`은 미발행 → 공식 `hack-font` 사용).
- 사이즈/라인하이트 스케일은 브랜드 페이지에 없음 → shadcn/Tailwind 기본 타입 스케일 사용.

### 우리 스택 현황 (`apps/web`)

- `layout.tsx`: `Geist`를 `next/font/google`로 로드 → `--font-sans` 주입. `hack-font/build/web/hack.css` import.
- `globals.css`:
  - 팔레트는 `:root` 하나. `.dark` 블록 없음.
  - `@custom-variant dark (&)` — 다크 단일이라 shadcn 컴포넌트의 `dark:` 유틸이 항상 적용되게 항등 처리.
  - `--font-mono` → `var(--font-mono)` (`:root`에서 `"Hack", …`). `--font-heading`은 `--font-sans` 공유.

---

## 범위 밖

이 문서는 팔레트(다크 전용)와 타이포만 정의한다. 아래는 없다:

- 라이트 모드 — 다크 단일, 대응 안 함
- 스페이싱·radius·shadow — shadcn/Tailwind 기본값 사용
- 컴포넌트별 스펙
